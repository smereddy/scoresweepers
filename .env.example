# Supabase Configuration
# Get these values from your Supabase project dashboard
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Optional: Custom domain for production
VITE_CUSTOM_DOMAIN=https://yourdomain.com

# Note: The following variables are for Supabase Edge Functions only
# and should be set in the Supabase dashboard, not in this file

# Stripe Configuration (Edge Functions only - DO NOT add VITE_ prefix)
# STRIPE_SECRET_KEY=sk_test_... (for testing) or sk_live_... (for production)
# STRIPE_WEBHOOK_SECRET=whsec_...

# OpenAI Configuration (Edge Functions only - DO NOT add VITE_ prefix)
# OPENAI_API_KEY=sk_... (your OpenAI API key)
# OPENAI_MODEL=gpt-4o (or another model like gpt-3.5-turbo)

# Development Notes:
# 1. Copy this file to .env and fill in your actual values
# 2. Never commit .env to version control
# 3. The app works in demo mode without these variables
# 4. Stripe and OpenAI variables go in Supabase Edge Function secrets, not here